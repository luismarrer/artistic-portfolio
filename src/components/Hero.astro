<section>
    <div
        class="relative
        min-h-screen
        flex
        flex-col
        items-center
        justify-center
        text-center
        px-4
        overflow-hidden
        grid-background"
    >
        <div class="z-10 flex flex-col items-center max-w-4xl mx-auto">
            <h1
                class="text-6xl md:text-8xl font-bold text-[#F9FAFB] leading-tight"
            >
                Ideas Visualized
            </h1>
            <div class="mt-10">
                <button
                    class="bg-[#B22222] text-[#FFFDD0] font-semibold py-3 px-8 rounded-lg shadow-lg hover:bg-[#A52A2A] transform hover:-translate-y-1 transition-all duration-300 text-lg"
                >
                    Start Brainstorming
                </button>
            </div>
        </div>
        <!-- Soft blended overlay at the bottom of the hero to smoothly transition into the Services section -->
        <div
            class="absolute left-0 right-0 bottom-0 pointer-events-none overflow-hidden z-20"
            style="height:260px;"
        >
            <svg
                viewBox="0 0 1440 320"
                preserveAspectRatio="none"
                class="w-full h-full"
                xmlns="http://www.w3.org/2000/svg"
                aria-hidden="true"
            >
                <defs>
                    <linearGradient
                        id="g-hero-to-services"
                        x1="0"
                        x2="0"
                        y1="0"
                        y2="1"
                    >
                        <stop
                            offset="110%"
                            stop-color="#FEEACA"
                            stop-opacity="1"></stop>
                        <stop
                            offset="55%"
                            stop-color="#FEEACA"
                            stop-opacity="0.6"></stop>
                        <stop
                            offset="100%"
                            stop-color="#FEEACA"
                            stop-opacity="0"></stop>
                    </linearGradient>
                    <filter id="blur-filter">
                        <feGaussianBlur stdDeviation="28"></feGaussianBlur>
                    </filter>
                </defs>

                <!-- simple bottom gradient overlay for smooth transition -->
                <rect
                    x="0"
                    y="200"
                    width="1440"
                    height="320"
                    fill="url(#g-hero-to-services)"
                    filter="url(#blur-filter)"
                    style="mix-blend-mode:normal;"></rect>
            </svg>
        </div>
    </div>
</section>

<style>
    body {
        background-image: linear-gradient(to bottom right, #ffdac1, #e57373);
        background-size: 100% 100%;
    }
    .grid-background {
        background-image: linear-gradient(
                to right,
                rgba(178, 34, 34, 0.2) 1px,
                transparent 1px
            ),
            linear-gradient(
                to bottom,
                rgba(178, 34, 34, 0.2) 1px,
                transparent 1px
            );
        background-size: 20px 20px;
    }
    .animate-bounce-slow {
        animation: bounce-slow 2s infinite;
    }
    @keyframes bounce-slow {
        0%,
        100% {
            transform: translateY(-15%);
            animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
        }
        50% {
            transform: translateY(0);
            animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
        }
    }
</style>
