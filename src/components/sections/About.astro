---
import Section from "@/components/Section.astro"

const aboutData = {
  section: {
    title: "About Me",
    description:
      "Digital artist and creative designer with a passion for transforming ideas into unique visual experiences",
    id: "about",
  },
  image: {
    src: "/images/profile.png",
    alt: "Sketch-style portrait of the artist",
  },
  bio: {
    heading: "Creating Digital Art Since 2020",
    paragraphs: [
      "I'm an artist passionate about new digital technologies. My work focuses on creating visual experiences that tell stories and connect with the viewer's emotions.",
      "With experience in graphic design, digital illustration, and creative direction, I've collaborated with various clients to bring their visions to life through art.",
    ],
  },
  skills: [
    "Digital Illustration",
    "Graphic Design",
    "SEO",
    "Animations",
    "UI/UX Design",
    "Conceptual Art",
  ],
  stats: [
    { value: 35, suffix: "+", label: "Projects" },
    { value: 30, suffix: "+", label: "Clients" },
    { value: 4, suffix: "+", label: "Years" },
  ],
}
---

<Section
  title={aboutData.section.title}
  description={aboutData.section.description}
  id={aboutData.section.id}
>
  <article
    class="max-w-6xl mx-auto gap-10 md:gap-8 lg:gap-20 grid md:grid-cols-2 items-center"
  >
    <!-- Image -->
    <div
      class="w-full max-w-56 sm:max-w-72 lg:max-w-96 mx-auto p-2 hover:rotate-3 transition-transform duration-300"
      data-reveal="left"
    >
      <img
        class="rounded-xl shadow-lg"
        alt={aboutData.image.alt}
        src={aboutData.image.src}
        style="border: 10px solid white; transform: rotate(-3deg);"
      />
    </div>

    <!-- Bio -->
    <div class="min-w-0 space-y-6" data-reveal="right">
      <h3 class="text-2xl sm:text-3xl font-bold text-gray-800 text-center md:text-left">
        {aboutData.bio.heading}
      </h3>

      {
        aboutData.bio.paragraphs.map((paragraph) => (
          <p class="text-gray-700 leading-relaxed">{paragraph}</p>
        ))
      }

      <!-- Skills -->
      <div class="pt-4">
        <h4
          class="text-xl font-semibold text-gray-800 mb-4 text-center md:text-left"
        >
          Main Skills
        </h4>
        <ul class="grid grid-cols-2 gap-3">
          {
            aboutData.skills.map((skill) => (
              <li class="flex items-center gap-2 text-gray-700">
                <span class="w-2 h-2 shrink-0 bg-[#B22222] rounded-full" />
                {skill}
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Statistics -->
      <div class="grid grid-cols-3 gap-4 pt-6 border-t border-gray-300" data-stats>
        {
          aboutData.stats.map((stat) => (
            <div class="text-center">
              <div
                class="text-3xl font-bold text-[#B22222]"
                data-count={stat.value}
                data-count-suffix={stat.suffix}
              >
                {stat.value}{stat.suffix}
              </div>
              <div class="text-sm text-gray-600">{stat.label}</div>
            </div>
          ))
        }
      </div>
    </div>
  </article>
</Section>
